spring:
  application:
    name: docker-test-server

  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:9000}
      failFast: true
      allowOverride: true
      overrideNone: true
      overrideSystemProperties: false

server:
  port: 8100
