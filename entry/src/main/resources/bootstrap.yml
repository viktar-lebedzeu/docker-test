spring:
  application:
    name: docker-test-entry

  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:9000}
      failFast: false
      allowOverride: true
      overrideNone: true
      overrideSystemProperties: false

server:
  port: ${SERVICE_PORT:8200}
